<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{title}}</title>
  </head>
  <body>
    {{{body}}}
    <script>
      // Conectar al WebSocket const ws = new WebSocket('ws://localhost:8080');
      ws.onopen = () => { console.log('Conectado al servidor WebSocket'); };
      ws.onmessage = (event) => { console.log('Mensaje recibido del servidor:',
      event.data); }; ws.onclose = () => { console.log('Desconectado del
      servidor WebSocket'); }; // Enviar un mensaje al servidor WebSocket
      function sendMessage(message) { ws.send(message); }
    </script>
  </body>
</html>